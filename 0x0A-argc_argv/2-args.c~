#include <stdio.h>
/**
 * main - sadfa
 * @argc: sadf
 * @argv: asdfa
 * Return: asdfa
 */
int main(int argc, char *argv[])
{
  int res;
  if (argc == 2)
    {
      res = argv[1] + argv[2];
      printf("%d \n", res);
    }else{
    printf("Error \n");
    return (1);
  }
  return (0);
}
