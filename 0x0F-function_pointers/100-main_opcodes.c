#include <stdio.h>
#include <stdlib.h>

/**
 * main - asdasdas
 *
 * @argc: asdasda
 * @argv: adasdas
 * Return: asdasds
 */
int main(int argc, char *argv[])
{
	int bytes, i;
	unsigned char *pf;

	if (argc != 2)
	{
		printf("Error\n");
		exit(1);
	}
	bytes = atoi(argv[1]);
	if (bytes < 0)
	{
		printf("Error\n");
		exit(2);
	}
	i = 0;


}
