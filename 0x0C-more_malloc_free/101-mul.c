#include <stdio.h>
#include <stdlib.h>

/**
 * main - entry point
 * @argc: count elements
 * @argv: elements
 * Return: always 0
 */

int main(int argc, char *argv[])
{
	long int num1, num2, mul;

	if (argc != 3)
	{
		printf("Error\n");
		exit(98);
	}
	return (0);
}
